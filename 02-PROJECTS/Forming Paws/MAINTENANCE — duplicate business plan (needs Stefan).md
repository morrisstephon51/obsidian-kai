---
created: 2026-09-23
type: maintenance-flag
status: needs-stefan
tags:
  - maintenance
  - forming-paws
---

# Maintenance flag — duplicate business plan at a corrupt path

**Found by automated vault-maintenance run, 2026-09-23. Needs a decision from Stefan — not auto-fixed because it involves deleting content and choosing between two versions.**

## The problem

Commit `a6248ac4` ("Add comprehensive business plan…") accidentally committed the business plan to a **broken path with literal backslashes baked into the filename**:

```
02-PROJECTS/Forming\ Paws/Business\ Plan\ —\ Grant\ Applications.md   ← STRAY (370 lines, ~2,525 words)
```

That created a phantom folder `Forming\ Paws/` (note the backslash) separate from the real `Forming Paws/`. Obsidian cannot cleanly link to or navigate it.

The next commit `9c7fae13` re-added the plan at the **correct** path and linked it from the Grants index:

```
02-PROJECTS/Forming Paws/Business Plan - Grant Applications.md        ← CANONICAL (240 lines, ~1,436 words, linked everywhere)
```

The stray copy was never removed.

## What's true right now

- **Nothing links to the stray file.** The Grants index, `README-PERPLEXITY.md`, and the "correct" `_INDEX.md` entry all point to the canonical `Business Plan - Grant Applications.md`.
- `_INDEX.md` (auto-generated by `node _ops/build-index.mjs`) still lists the stray file on its own line because the file physically exists. That entry will disappear on the next index rebuild **once the stray file is gone**.
- The two files are the **same** business plan, but **not identical**: the stray copy is a longer/fuller first draft (~1,000 more words); the canonical one is a condensed version. Neither is a strict subset of the other.

## Why this wasn't auto-fixed

Deleting a tracked file with unique content, and deciding which of two versions of a document to keep, is an editorial judgment — the maintenance agent's safety rules say to flag that, not guess.

## Suggested resolution (pick one)

1. **Keep the condensed canonical file as-is** (it's the one already linked). Then delete the stray:
   ```sh
   git rm "02-PROJECTS/Forming\\ Paws/Business\\ Plan\\ —\\ Grant\\ Applications.md"
   node _ops/build-index.mjs   # rebuild _INDEX.md
   ```
2. **Prefer the fuller draft.** Copy any wanted extra detail from the stray copy into `Business Plan - Grant Applications.md`, then delete the stray as in option 1.

Either way, finish by deleting **this flag note** and rebuilding `_INDEX.md`.
