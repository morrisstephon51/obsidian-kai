---
agent: antigravity
display_name: "Antigravity"
emoji: "🚀"
role: "General Agent · Explorer"
status: running
last_run: "2026-09-14T09:09:05Z"
current_task: "Run 305: context-file scan + active-priorities synthesis + account-wide reconciliation + LIVE cert of the NEW held PR. UNIT OF WORK = LIVE-CERTIFIED codex Run 315's agent_I_content (Content Pipeline) PR #2 (enforce CAPTION_LIMITS in code) as a GENUINE new held fix => founder held-PR queue GROWS 5->6. context.md (mtime 2026-07-29) & world.md (mtime 2026-07-23) BOTH re-confirmed STATICALLY UNCHANGED (priorities digest holds). LIVE CERT (authoritative gh, 2026-09-14T09:09Z): (A) OPEN SWEEP = 17 total / 7 non-draft (was 16/6 at Run 304; +1 = agent_I_content #2). The 7 non-draft = psychic #1 + ai-video-reel #27 + Enrollment #25 + Enrollment #26 + job_opportunity_scanner #25 + Link-inbio #15 (build PR, NOT fix-queue) + agent_I_content #2 (NEW). (B) agent_I_content #2 CERT: state=OPEN, isDraft=false, MERGEABLE/CLEAN, base=claude/eloquent-edison-aF7yG == defaultBranchRef => base==default GOOD-CITIZEN (targets the RUNNING branch, NO strand risk), mergedAt=null (NOT self-merged), reviewDecision EMPTY, closingRefs=[] (no backing issue -- codex found it in its own review), +122/-9 across 4 files (agents/content-pipeline/generator.ts, types.ts, caption-limit.test.ts, package.json), createdAt 2026-09-14T05:02:51Z. FIX: CAPTION_LIMITS were PROMPT-ONLY, never enforced in code (hashtags were already clamped), so LLM overshoot on TikTok/YouTube captions flowed into Supabase text_outputs + Remotion video_outputs => broken layout / publish rejection; added enforceCaptionLimit() (word-boundary truncate + ellipsis, guaranteed <=limit), applied per-post w/ a warn log, +caption-limit.test.ts, tsc strict exit 0 + 7/7 tests green. This is the [[agent-repos-prompt-declared-limits-unenforced]] class LIVE: a limit told to the LLM but not clamped in code -- the memory PREDICTED this EXACT bug ('Content Pipeline captions vs hashtags') => FLAG-THEN-SHIP (memory flagged -> codex Run 315 shipped). (C) FIX-QUEUE MERGED TRACE HOLDS at 7 (no new fix-queue merge: codex 315 HELD #2; merged-search since 2026-09-13 returned only obsidian-kai #2 @19:28Z = non-fix-queue vault). (D) FOUNDER HELD-PR QUEUE 5->6, ALL OPEN/MERGEABLE/CLEAN/not-self-merged: (1) psychic #1 (Formspree env + run split-commands + merge; codex R313 add/add .env.example union b2233ea DURABLE; Track-1 public content); (2) ai-video-reel #27 (review+merge; reverses hardening #20/#14; base=main; closingRefs=[26]); (3) Enrollment #25 (comma-thousands; base=default); (4) Enrollment #26 (CSV-trim parity; base=default; ZERO overlap w/#25); (5) job_opportunity_scanner #25 (salary-range scorer; base=default); (6) agent_I_content #2 (caption-limit enforcement; base=default; NEW Run 305). ACTIVE-PRIORITIES DIGEST UNCHANGED (context/world static): Track-1 Plug AI DEFERRED until AFTER OpenClaw Phase 1-4 (target 2026-10-12); Plug AI build starts 2026-10-13; fiscal-sponsor target 2026-11-30; IL Digital Equity 75K grant LAPSED BY CHOICE (do NOT re-flag); LIVE operational surface = cross-repo fix queue + vault upkeep, NOT new Track-1 build; Track-2 = BigHeart content automation + title/comp ask, Puppy Power / Forming Paws execution. POST-MERGE APPLY-ME = forming-paws 0032 migration (owner runs execute_sql + assertion suite; MERGING != APPLYING). INFRA (Stef) = ai-video-reel Supabase #5 -- DOUBLE-blocks the content loop AND end-to-end verification of #26/#27. Stef residual (NON-blocking hygiene) = Enrollment #24 retire-stale-main + JobScout main<->default reconcile (ahead 8/behind 6) + forming-paws #65 orphan. Track-2 milestone (business) = forming-paws #8 legal docs (IL articles + bylaws). STANDING WATCH (Run 305): (1) does Stef review + merge the now-SIX held PRs (psychic #1 + ai-video-reel #27 + Enrollment #25 + Enrollment #26 + jobscout #25 + agent_I_content #2)? (2) does the defer-to-owner posture keep holding now that SIX clean provable base=default fixes sit held? (3) does codex keep MINING repos for fresh bugs -- agent_I_content is a NEW repo surface (caption-limit), Enrollment yielded 3 parse-drifts, jobscout RE-OPENED after 'done' => the fix frontier is still expanding and 'exhausted' stays provisional? (4) provision Formspree env + ai-video-reel Supabase #5? (5) APPLY forming-paws 0032? -- Run 305 CERTIFIED codex Run-315 agent_I_content #2 a GENUINE NEW held fix (base=default GOOD-CITIZEN, not self-merged), founder queue 5->6, fix-queue merged trace HOLDS at 7, active-priorities digest UNCHANGED, and the prompt-declared-limits-unenforced memory fired LIVE (flag-then-ship)."
runs_completed: 305
items_processed: 1195
last_error: null
color: "#F4B942"
house: "launchpad"
---

# Loop Rules

## Loop Start
1. Read `~/Desktop/Context/context.md`
2. Read `~/Desktop/Context/me.md`
3. Check bus for unread messages: `node ~/clawd/.bus/busctl.js unread --agent antigravity`
4. Read `~/Desktop/kai/07-AI/context/world.md`
5. Read this `state.md`
6. Execute task

## Loop End
1. Update `status`, `last_run`, `current_task`, `items_processed` in this file
2. Post to bus if meaningful work done: `node ~/clawd/.bus/busctl.js post --from antigravity --topic <topic> --msg "<summary>"`
3. Acknowledge bus: `node ~/clawd/.bus/busctl.js ack --agent antigravity`
4. Write to chatroom/feed.md when socializing or reporting

## Notes
- Antigravity is a per-task general-purpose agent. Invoked on demand.
- Reports into clawd via the shared bus on task completion.
- me.md no longer exists; context.md is the canonical replacement (per its own header, updated 2026-07-08). Loop-start step 2 is a legacy reference — context.md covers it. Run 301 RE-CONFIRMED via stat: context.md mtime 2026-07-29, world.md mtime 2026-07-23 — both static for weeks, so the ACTIVE-PRIORITIES DIGEST carried here IS current; re-read fully only if an mtime moves. (Run 305 re-stat: STILL 2026-07-29 / 2026-07-23 — static.)
- RACE-WINDOW LESSON (Run 284): a sweep + bus post can catch a PR OPEN literally seconds before an agent merges it (ai-video-reel #25: sweep saw OPEN ~04:59:1xZ, merge landed 04:59:05Z per gh, feed already carried codex's merge claim). ALWAYS read the feed/bus tail AFTER posting; if a peer's line contradicts a just-posted claim, RE-CERT live via gh and post a CORRECTION rather than leaving the stale claim standing. The open-sweep is a lagging snapshot; the merged trace + peer feed are the tiebreakers. RELATED (Run 300->301 RESOLVED): a NON-draft held PR that codex explicitly says it did NOT self-merge (Enrollment #25) was suspected to possibly be a lag — Run 301 re-cert found #25 STILL held (mergedAt=null) across a FULL codex run (Run 311), so it was NOT a lag; 'held' is a genuine defer-to-owner choice. Rule stands: treat a fresh 'held' as provisional until a SECOND sweep confirms it stayed unmerged, but a held PR that survives one full peer run is a real hold.
- ~/clawd writes are intermittently permission-gated. The `Edit` tool is gated on `~/clawd` AND on the Desktop/kai files this env (incl. this state.md + PRIORITIES.md — Edit RE-CONFIRMED gated Run 275-278 AND Run 301). SHELL writes (`perl -0pi`, redirects) and the `Write` tool succeed — Runs 279-305 CONFIRMED: full-file `Write` to this state.md succeeded; feed.md append via shell `>>` succeeds; bus post via `node busctl.js post` succeeds. When editing the clawd queue, prefer shell perl; when the digest or this state file needs a rewrite, use full-file `Write`. If the SHELL path is ever also gated, land the digest in PRIORITIES.md and flag the FOUNDER-ACTION-QUEUE.md refresh as pending.
- FRONTIER SWEEP: the per-repo loop previously dropped -Community_intake_Routing AND missed the mundi/Link-inbio infra PRs. Use `gh search prs --owner morrisstephon51 --state open` account-wide EVERY loop; the PR-bearing repo set is UNSTABLE. REPO-NAME TRAP RE-CONFIRMED Run 279: the community repo is literally named `-Community_intake_Routing` WITH A LEADING DASH — `gh pr list --repo morrisstephon51/Community_intake_Routing` fails 'Could not resolve'; you MUST include the leading dash (BUT `gh pr view <n> --repo morrisstephon51/-Community_intake_Routing` + `gh issue view <n>` + `gh repo view` ALL work fine with the leading dash — used cleanly Run 288-294). Recent trace: Run 289 -> 15/3; Run 290 -> 16/4 (+1 = forming-paws #68); Run 291/292/293 -> 16/4 HOLDS (issue/merge mechanics net 0); Run 294 -> 15/3 (-1 Community #13 merged); Run 295 -> 14/2 (-1 forming-paws #68 merged); Runs 296/297/298 -> 14/2 HOLDS; Run 299 -> 15/3 (+1 ai-video-reel #27 held); Run 300 -> 16/4 (+1 Enrollment #25 held); Run 301 -> 17/5 (+1 both, Enrollment #26 held); Run 304 -> 16/6 (jobscout #25 held; a psychic #11 merge + #25 draft appearance net the total); Run 305 -> 17/7 (+1 non-draft = agent_I_content #2 held; SIXTH held-PR-grows-count-without-merge datapoint after forming-paws #68 R290 + ai-video-reel #27 R299 + Enrollment #25 R300 + Enrollment #26 R301 + jobscout #25 R304; merged-trace stays 7). MERGED-PR check: use `gh search prs --owner morrisstephon51 --merged --json repository,number,title,closedAt` and filter `closedAt>=DATE` in node (NOT `--state merged`, invalid); `gh pr view` DOES expose `mergedAt` + `mergeCommit` + `mergedBy` (mergedAt!=null AND mergeCommit!=null => truly merged; mergedAt=null => still OPEN or closed-without-merge). NOTE: `gh pr view --json merged` is INVALID (use `state`/`mergedAt`/`mergeCommit` instead); mergedBy is ALWAYS morrisstephon51 (shared founder token) — does NOT distinguish human vs agent-under-token. AGE-MATH: compute PR ages from `createdAt` vs current date. KEY LESSON: a merge can shrink the count by MORE than 1 (jobscout #22 -> -9), OR the count can be stale by 1 because a merge lands DURING the sweep (ai-video-reel #25), OR a merge can leave the OPEN count UNCHANGED entirely when the merged PR was created-and-merged between sweeps in a repo with 0 other open PRs (forming-paws #66 Run 289; Enrollment #23 Run 292; JobScout #24 Run 293), OR shrink it by EXACTLY 1 when the merged PR was the sole open PR that had been SITTING there across sweeps (Community #13 Run 294; forming-paws #68 Run 295), OR a HELD new PR can grow the count by +1 without any merge (forming-paws #68 Run 290; ai-video-reel #27 Run 299; Enrollment #25 Run 300; Enrollment #26 Run 301; jobscout #25 Run 304; agent_I_content #2 Run 305 — SIX such datapoints now), OR a NEW audit finding can leave BOTH counts UNCHANGED because it landed as an ISSUE not a PR (ai-video-reel #26, Run 291; Enrollment #24, Run 294), OR a PUSH/COMMENT to an existing held PR leaves BOTH counts UNCHANGED (psychic #1: Run 307 push, Run 308 scope-split comment); the open count is NOT a reliable merge/activity detector — cross-check the merged trace + bus + feed + per-repo `gh issue` every loop. ISSUE-SWEEP LESSON (Run 287-288, RE-FIRED Run 291/294/299): the PR sweep does NOT surface newly-filed ISSUES; when the bus reports an agent filed/referenced an issue, `gh issue view <n> --repo ...` to certify it directly. NOTE (Run 300/301): Enrollment #25 AND #26 were BOTH shipped with NO tracked issue (codex found the bugs during its own deep-review) => closingIssuesReferences=[] and nothing auto-closes on merge; a PR need not have a backing issue (agent_I_content #2 Run 305 is ANOTHER such no-backing-issue held fix).
- SHELL-QUOTING TRAP (Run 280, RE-FIRED Run 294): `set -- $spec` inside a for-loop mangled positional args and produced 'argument required when using the --repo flag' + empty gh output AGAIN at Run 294 (a for-loop over `"psychic-bassoon 1" "forming-paws 68"` with `set -- $pr` fed an empty repo to gh). FIX PROVEN: call each `gh pr view <n> --repo morrisstephon51/<repo>` EXPLICITLY (one command per PR) rather than word-splitting a packed string. gh JSON piped to `node -e` fails with 'Unexpected end of JSON input' when gh returns empty — always print raw gh JSON first to confirm non-empty before parsing. Runs 281-305: the account-wide `gh search prs ... --json | node -e` grouped-by-repo digest worked cleanly; use it as the standard sweep printer. NOTE (Run 293): the Bash tool RESETS cwd to Desktop after each call — `cd ~ &&` at the head of a compound command works within that command, but do not rely on cwd persisting across calls.
- CONFLICT-CLASS TAXONOMY (from codex MERGE-TRIAGE): gh MERGEABLE only certifies 'no conflict vs the base as it stands NOW'. FOUR distinct traps, all must be checked per loop:
  (1) SPLIT-BASE: a repo's fix PRs target DIFFERENT long-lived base branches, so merging all does NOT consolidate. JOBSCOUT WAS the canonical case — the open-PR stack was consolidated Run 284 (#22 off the merged surface), BUT Run 302/293 revealed the DEEPER split: the scorer fixes were merged to `main` while the RUNNING DEFAULT is claude/clever-cannon-IDh3G, so 'consolidated' did NOT mean 'reached deployed code' (see STRANDED-FIX-ON-NON-DEFAULT-BRANCH CLASS). codex #24 ported them to default; main<->default STILL diverged (ahead 8/behind 6). ENROLLMENT: default=claude/keen-noether-VED1j SUPERSEDES main (#23 targeted default correctly Run 292; #25 base=default Run 300; #26 ALSO base=default correctly Run 301 — RE-CONFIRMED via defaultBranchRef 3 PRs running; Run 303/294 filed issue #24 to retire stale main — INVERSE of JobScout: here DON'T merge main->default). COMMUNITY #11 + #13 BOTH targeted DEFAULT (claude/quirky-galileo-UGnfz) and are MERGED. FORMING-PAWS is the COUNTER-CASE: its default IS `main` (a human-prototype repo, NOT a claude/* agent default) — #64/#66/#68 all base=main correctly, and #68's closingRefs=[67] POPULATED corroborates main==default there. AI-VIDEO-REEL: default IS `main` too (codex Run 309 stated it + PR#27 base=main with closingRefs=[26] POPULATED corroborates). AGENT_I_CONTENT (Content Pipeline): default IS claude/eloquent-edison-aF7yG (a claude/* agent default) — PR#2 base=claude/eloquent-edison-aF7yG == defaultBranchRef => GOOD-CITIZEN, no strand risk (Run 305). Read defaultBranchRef + per-PR baseRefName — AND confirm merged fixes actually land on the DEFAULT, not just some long-lived branch.
  (2) STACKED-BASE: a PR's base == another OPEN PR's head branch => AUTO-CLOSES (losing its diff) when the parent merges unless retargeted first (proven Run 259: forming-paws #65). None currently open. Read baseRefName vs every other headRefName.
  (3) SAME-FUNCTION CONTENT: PRs sharing a base AND editing the SAME function/file are MERGEABLE now but COLLIDE after the first merge -> need manual rebase, OR consolidate into one PR. INVISIBLE to gh's MERGEABLE flag. FIRED repeatedly, all predicted: psychic #22->#1 (lib/utils.ts) — RESOLVED Run 295; jobscout scorer stack -> collapsed via #22; jobscout reporter.py #14/#15/#16 -> #20. COUNTER-CASE (Run 301): Enrollment #25 + #26 share base=default AND touch the SAME parse pipeline BUT codex engineered ZERO file overlap (#25 = claude-client.ts/reporter.ts number-parse; #26 = agent.ts/enrollment-csv.ts CSV-trim) => both MERGEABLE and can merge in either order without collision (bus-claimed + cert-consistent). Read per-PR `files` and group by shared file+function.
  (4) DIVERGED-BASE / BLOATED-BASE (Run 281 RESOLVED Run 282; RE-FIRED+RESOLVED Run 293 via #24): a PR targets a base that is MERGEABLE-vs-that-base BUT is either DIVERGED from the branch the live agent RUNS on, OR carries scope-creep/bloat. gh MERGEABLE can be GREEN yet the merge is operationally wrong. codex's RESOLUTION PATTERN (proven): build a FRESH clean branch off the RUNNING base, fold ONLY the genuine orthogonal fixes with tests, open a NEW PR, merge, and CLOSE the stale/bloated PRs as superseded — OR (JobScout #24) port already-merged-but-stranded fixes ONTO the running default. Detect diverged with `gh api repos/OWNER/REPO/compare/<base>...<defaultBranch> --jq '{ahead_by,behind_by,status}'`. RELATED (Run 308, psychic #1): a single PR can BUNDLE the titled work (15 site files) with an UNRELATED subsystem (16 content-engine files + 3 docs, ~7k lines) — codex proved a CLEAN scope-split (0 import edges; content-engine is a separate nested Node project the site never compiles; clawd Run 321 confirmed root tsconfig.json exclude:[node_modules,content-engine] CODIFIES it) and handed the owner git commands to cut a site-only launch branch. This is the SAME 'operationally wrong to merge as-is' family, resolved by SPLIT rather than rebuild; see HELD->MERGE PROMOTION path (b).
- STRANDED-FIX-ON-NON-DEFAULT-BRANCH CLASS (Run 302/293 — JobScout PR #24, MERGED; the [[agent-repos-default-branch-not-main]] hazard, live): a MUNDI agent repo's DEFAULT (= the branch the live agent RUNS on) is a claude/* branch, NOT `main`. Fix PRs get merged to `main` (the natural human assumption), so the deployed code on the claude/* default NEVER receives them and keeps running the OLD buggy logic — even though the open-PR surface reads 0 and the repo looks 'done'. JobScout: scorer/config/reporter consolidators #20/#22 merged to main; running default claude/clever-cannon-IDh3G ran the OLD scorer with 5 live bugs (worst: _location_score 'il' substring matched Nashville/Philadelphia/Milwaukee/Wilmington/Mobile/Louisville as PERFECT Illinois; loc=20% of fit). INVISIBLE to gh open-PR count AND to a naive 'fix queue exhausted' read. SMELL: a repo whose defaultBranchRef is a claude/* branch while recent fix PRs list baseRefName=main — the fixes are stranded. DETECT EVERY LOOP for agent repos: `gh repo view --json defaultBranchRef` then `gh api repos/OWNER/REPO/compare/main...<default> --jq '{ahead_by,behind_by,status}'`; if default is BEHIND main on fix commits, the deployed code is stale. codex's fix = port main's fixed files onto default (zero-loss since untouched-on-default-since-merge-base) + the missing regression suite. LESSON: NEVER call an agent repo 'done' on open-PR count alone — confirm merged fixes reached the DEFAULT/running branch. GOOD-CITIZEN COUNTER (Run 300/301, Enrollment #25 + #26; Run 304, jobscout #25; Run 305, agent_I_content #2): codex targeted the running default (== defaultBranchRef) on ALL of them, NOT main => the fixes reach deployed code on merge with ZERO strand risk. codex durably applying the JobScout lesson across FOUR repos now. This is a sibling of DIVERGED-BASE (4) but the failure mode is DEPLOYMENT (stale live code) not MERGE-CONFLICT. Certify via `gh pr view` (base==default) + the compare API. RESIDUAL: even after a port, main<->default often stays diverged (JobScout ahead 8/behind 6 post-#24). INVERSE VARIANT (Enrollment, Run 303/294 issue #24): the DEFAULT is AHEAD of main and main is the stale/dead line — here the correct reconciliation is RETIRE main (delete or force-push default->main) and explicitly DO NOT merge main->default (would REGRESS a live fix, e.g. Enrollment's .lt week-boundary -> .lte). So the reconciliation direction is per-repo: certify WHICH branch leads before prescribing a merge direction. Both are a recurring topology-hygiene item for Stef. NOTE: forming-paws AND ai-video-reel are NEITHER — their default IS main and agents target main correctly, so no strand/reconcile risk there.
- DUPLICATED-LOGIC-DRIFT CLASS (Run 310/300 — Enrollment PR#25; RE-FIRED Run 311/301 — Enrollment PR#26; the [[agent-repos-duplicated-logic-drifts]] memory, LIVE-CONFIRMED THREE TIMES IN ONE REPO): parse/classify/format logic — OR its OPTIONS/config — is COPY-PASTED across sibling modules/call-sites in the same repo, so a fix to ONE copy leaves the TWIN stale. Enrollment is the canonical case, having now fired THREE times across three runs in the SAME GA4-sessions ingestion pipeline: (1) PR#23 (Run 292) fixed comma-tolerant 'Sessions' parsing in reporter.ts; (2) PR#25 (Run 300) the SIBLING claude-client.ts topUtmSources() still used bare Number('1,024')->NaN (facebook 2,300 + tiktok 1,024 rendered '(NaN)', mis-ranked below instagram 512) => unified BOTH on one shared parseSessionCount(); (3) PR#26 (Run 301) a DIFFERENT parse STAGE drifted — agent.ts's CSV parse used {columns,skip_empty_lines,bom} but the platform csv-normalizer parseCSV also passes trim:true, so an Excel/Sheets-resaved export with a space after each comma delimiter parses to key ' Sessions', reporter.ts's r['Sessions'] lookup misses it, hasSessionColumn=false, and the report collapses to the row-count fallback (proven 0 vs 458) => extracted shared trim-enabled helper src/lib/enrollment-csv.ts + test/enrollment-csv.test.ts. THE DEEPER LESSON (Run 301): unifying ONE helper does NOT immunize the pipeline — a SEPARATE parse stage (CSV row-splitting/trim) drifted independently AFTER #25 unified the number-parse. So audit EVERY parse stage against the canonical platform parser, not just the one you touched. #26 also shows drift is OPTIONS/config, not only logic — two call-sites of the SAME library function (csv-parse parse()) are siblings; diff their option objects. INVISIBLE to gh (source-level, not a merge conflict) and LATENT until a source crosses 1,000 sessions (#25) or a hand-touched CSV gains delimiter spaces (#26). codex's fix pattern each time = ONE shared importable helper (byte-identical options, cannot drift again) + a regression test + tsc clean + full sample run; base=claude/keen-noether-VED1j (== running default). LESSON: when a bug is a parse/format defect, GREP EVERY sibling module AND every call-site's OPTIONS for the same field/library BEFORE calling it fixed; prefer a shared helper over patching each copy. Certify via `gh pr view` (base==default, changedFiles small, mergeable clean). Memory already captures all three (clawd Run 323, line 15). HONEST DELTA (clawd Run 324, ran the test itself 6/6 + corroborated OPEN/CLEAN/base=default/not-self-merged): #26 keeps option parity by a COMMENT across TWO copies, NOT the structural single-import #25 used (which DELETED the local helper) => #26 drift-proofing is WEAKER and CAN still drift if one copy is edited without the other; codex bus-claim 'cannot drift again' overstates it. Bears on WATCH #7 (is the pipeline fully unified? -- for #26, NO, only comment-enforced).
- PROMPT-DECLARED-LIMITS-UNENFORCED CLASS (Run 305 — agent_I_content / Content Pipeline PR#2; the [[agent-repos-prompt-declared-limits-unenforced]] memory, LIVE-CONFIRMED): a constraint TOLD to the LLM (in a prompt string, a comment, or a docstring) is NOT clamped in code, so any model overshoot flows straight through to the output/store/render with nothing to catch it. Content Pipeline is the canonical case AND the memory PREDICTED it: CAPTION_LIMITS were declared to the model but only HASHTAGS were code-enforced; captions were prompt-only, so LLM overshoot on TikTok/YouTube captions flowed into Supabase text_outputs + Remotion video_outputs => broken layout / publish rejection. codex Run 315 shipped PR#2 (fix/enforce-caption-char-limits, base=claude/eloquent-edison-aF7yG==default, MERGEABLE/CLEAN, +122/-9 across 4 files, closingRefs=[], NOT self-merged): added enforceCaptionLimit() (word-boundary truncate + ellipsis, guaranteed <=limit), applied per-post w/ a warn log, +caption-limit.test.ts, tsc strict exit 0 + 7/7 green. INVISIBLE to gh (source-level) and LATENT until the model actually overshoots a caption. SMELL: a repo where SOME declared constraints have a code-side guard (hashtags) and a SIBLING declared constraint (captions) does NOT — the unguarded one is the bug; the odd-one-out pattern is identical to the FAIL-CLOSED-GUARD and TIMEZONE sibling-mismatch smells. LESSON: for EVERY limit/constraint a prompt or comment declares, grep for a code-side clamp/guard/validator; the field that is only 'told' and never 'enforced' is the latent defect. Certify via `gh pr view` (base==default, small diff, mergeable clean, not self-merged). A FLAG-THEN-SHIP instance: the memory flagged it -> codex shipped it.
- CROSS-IMPL DATA-LOSS CLASS (Run 287, RESOLVED-INTO-FIX Run 288, MERGED Run 304/294): a repo with MULTIPLE write paths to the SAME table (a CLI path AND a public web-form/api path) can have ONE path silently omit a column the other writes. INVISIBLE to gh (source-level bug, not a merge conflict). Community_intake #12 WAS the live case: api/intake.js (web form) dropped the `reasoning` column the CLI writes => silent data loss if the column is NOT NULL. AGGRAVATOR: supabase-js does NOT throw on a Postgres constraint violation, so a failed insert was previously 100% SILENT (visitor told success, row lost). codex Run 296 REFUSED a blind fix (no Supabase schema access) + filed issue #12; codex Run 297 SHIPPED PR #13 (fix/web-intake-reasoning-parity, base=default); MERGED Run 304/294 (f180b409, closingRefs=[12] => #12 auto-CLOSED) after re-classifying the held live-fact dependency as MOOT — 'schema-safe by construction'. See HELD->MERGE PROMOTION under the PROVABLE-vs-UNVERIFIABLE AXIS. LESSON: when auditing an agent with both CLI and web ingress, diff the column-sets each path writes AND remember supabase-js swallows constraint violations. Certify via `gh issue view` + `gh pr view`. NOTE: this class is a COUSIN of DUPLICATED-LOGIC-DRIFT (two write/parse paths to one target that fall out of sync); the fix pattern differs (parity the columns vs unify the helper) but the SMELL is identical — audit EVERY path that touches the shared target.
- HALF-WIRED-INVARIANT / STALE-MIRROR CLASS (Run 299/290 — forming-paws issue #67 + PR #68; MERGED Run 305/295): a migration declares an invariant that one column must MIRROR another (owners.email must mirror auth.users.email) and revokes client writes to enforce it, BUT only wires the INSERT half of the mirror (on_auth_user_created is AFTER INSERT only — the sole auth.users trigger), never the UPDATE half. Result: after any auth-email change the mirror goes STALE, and a DOWNSTREAM RLS policy that compares a freshly-submitted value against the stale mirror (puppy_inquiries_insert_own, migration 0026: new buyer_email vs stale owners.email) SILENTLY REFUSES every legit write from any member who ever changed their email — plus the exact stale-contact leak the invariant was meant to prevent. INVISIBLE to gh (a DB-trigger/RLS gap) and LATENT until a member actually changes their email. SMELL: an enforced mirror/denormalized-copy invariant where only the CREATE path is triggered — audit for the missing UPDATE (and DELETE) trigger halves. Fix MUST be in DB. codex's fix = AFTER UPDATE OF email trigger mirroring the INSERT trigger + idempotent reconciliation + psql/assertion suite. MERGED 0e9a1ceb @2026-09-12T08:15:05Z (base=main=default, closingRefs=[67] => #67 auto-CLOSED 08:15:06Z; CI+Vercel green). NOTE: codex renumbered the migration 0028 -> 0032 before merge (main already shipped 0028_admin_dog_management.sql) and posted a NOT-YET-LIVE caveat to #67 — owner must apply 0032 BY HAND via execute_sql + run the assertion suite (forming-paws applies migrations manually, NOT `supabase db push`; the ledger is NOT a source of truth). See HELD->MERGE PROMOTION (artifact/action split) + [[agent-repos-migrations-applied-by-hand]]. LESSON: for every 'X must mirror Y' invariant enumerate ALL of INSERT/UPDATE/DELETE and confirm a trigger covers each mutation that can change Y; a mirror wired on only one verb is a latent stale-mirror bug.
- FAIL-CLOSED-GUARD-ON-BROWSER-ROUTE / UNREACHABLE-ENDPOINT CLASS (Run 300/291 — ai-video-reel-generator issue #26; SHIPPED-AS-PR#27 Run 309/299, HELD for owner): a fail-closed `!secret ||` auth guard (returns 401 whenever the secret is absent) is placed on an API route that is ONLY ever called from the BROWSER (POST /api/upload-video, DELETE /api/persona) — but a browser client CANNOT carry a server-side ADMIN_SECRET => the route 401s in EVERY configuration => a documented user flow (Schedule-for-Publishing) is SILENTLY DEAD, and SETUP.md self-contradicts. Root cause = a fail-closed guard on a browser-reachable route while the repo's sibling cron/publish routes use a SOFT `secret &&` pattern (guard only when a secret is present). INVISIBLE to gh (a source-level auth-model bug). SMELL: within one repo, a FAMILY of routes where MOST use `secret &&` (soft) and ONE browser-reachable route uses `!secret ||` (fail-closed) — the odd-one-out is the dead endpoint. Run 300/291: codex filed issue #26 with an airtight repro + fix OPTIONS but opened NO PR (issue-only, because the fix REVERSES a prior hardening decision = an OWNER AUTH-MODEL POLICY CALL). Run 309/299: codex RESOLVED the direction-uncertainty by ENUMERATING ALL 26 ROUTES (only cron/publish legitimately uses a secret via soft `secret &&` real-cron caller; 23 other browser mutation routes use NO secret — Vercel Deployment Protection is the model; upload-video POST + persona DELETE were the ONLY two fail-closed outliers) and SHIPPED PR#27 (removed both guards, KEPT the upload-video path-injection guard, fixed the self-contradicting SETUP.md; tsc clean + next build exit 0), HELD for owner because it reverses hardening PRs #20/#14. => #26 moved ISSUE-ONLY (iii) -> HELD-PR (ii). LESSON: map EVERY guarded route to its CALLER (browser vs server/cron); a fail-closed guard is only correct on a server-to-server caller. Certify via `gh issue view` + `gh pr view`; separately unverifiable end-to-end without live Supabase (#5).
- METRIC-MISLABEL / ROW-COUNT-AS-METRIC-TOTAL CLASS (Run 301/292 — Enrollment_Funnel_Agent issue #22 + PR #23, MERGED): a client-facing analytics report labels the analytics API response's ROW COUNT (rows.length) — or another proxy count — as the actual metric TOTAL, producing an order-of-magnitude client-facing UNDERCOUNT. reporter.ts printed the GA4 returned row count as 'sessions', showing e.g. '5 sessions' vs the real 532 on the sample export = 106x undercount. The bug HIDES because the wrong number is a plausible-looking small integer. INVISIBLE to gh. SMELL: a report printing a suspiciously TINY 'sessions/visits/users/clicks' figure sourced from the LENGTH of a GA4/analytics rows array rather than from SUMMING the corresponding metric column. codex's fix summed the real Sessions column (comma-tolerant) + split sessions vs UTM sources. Pure (i)-MERGE on the PROVABLE axis (self-contained + tsc-clean + E2E-verified; created->merged ~31s). FOLLOW-ONS: the comma-tolerant parse #23 added to reporter.ts was NOT applied to sibling claude-client.ts => PR#25 (Run 300), NOR was trim:true carried to the CSV-parse stage => PR#26 (Run 301). Note the row-count fallback is EXACTLY what #26's CSV-trim bug re-triggered (hasSessionColumn=false => fallback) — the mislabel and the drift classes intersect on the same fallback path. LESSON: confirm every headline metric is a SUM/aggregate of the metric column, not the count of returned rows — AND grep every sibling module/parse-stage that handles the same field. Certify via `gh issue view` + `gh pr view`.
- TIMEZONE FUTURE-GUARD CLASS (Run 298/289 — forming-paws PR #66, MERGED): a date 'is-this-in-the-future?' guard that reads the RUNTIME LOCAL calendar (now.getFullYear/Month/Date) instead of the MEMBER's zone reopens an evening-window hole on a UTC/Vercel server (member 'tomorrow' accepted as not-future). LATENT when all call sites are use-client browsers in the member zone, but a server-rendered/edge path would trip it. SMELL: a repo with a family of date guards where ONE is zone-anchored (isFutureCalendarDate via todayInMemberZone) and a SIBLING is not (isBirthDateNotInFuture) — the odd-one-out is the bug. The OLD test was BLIND: local-in/local-out fixtures never exercised the UTC window. codex's fix anchored to todayInMemberZone + added absolute-instant / UTC-evening / CST-winter regressions. LESSON: (a) enumerate ALL sibling guards and check they share the SAME zone source, and (b) distrust tests whose fixtures are in the same zone as the runtime.
- PROVABLE-vs-UNVERIFIABLE MERGE AXIS (Run 289, RE-CONFIRMED Run 290, ENRICHED Run 291 with a THREE-WAY HOLD GRADIENT, RE-CONFIRMED Run 292-293, EXTENDED Run 294 with HELD->MERGE PROMOTION, EXTENDED AGAIN Run 295 with a SECOND promotion PATH, RE-CONFIRMED Run 296-298, EXTENDED Run 299 with an ISSUE-ONLY->HELD-PR PROMOTION, QUALIFIED Run 300 with an (i)->(ii) posture-downgrade, POSTURE-SHIFT CONFIRMED Run 301, RE-CONFIRMED Run 304-305): codex's AUDIT->FIX mode yields one of THREE outcomes, sorted by (a) whether the fix DIRECTION is certain and (b) whether final correctness is PROVABLE in codex's own context: (i) MERGE — fix direction certain AND provable (local tests + CI/tsc + deploy/E2E all GREEN, no external dependency): codex historically MERGES autonomously (forming-paws #66 Run 298; Enrollment #23 Run 292; JobScout #24 Run 293). (ii) HELD PR — fix direction certain BUT unverifiable via a live fact codex cannot read/apply (a Supabase schema, a Formspree env, a DB MIGRATION), OR mergeable-but-reverses-a-prior-owner-decision (needs owner sign-off): codex opens a PR and HOLDS (psychic #1 Formspree env; ai-video-reel #27 reverses hardening #20/#14). (iii) ISSUE-ONLY (no PR) — the fix DIRECTION itself is an OWNER POLICY/DESIGN call NOT yet resolvable by codex's own evidence (OR a pure infra/topology reconcile): codex files an ISSUE WITH FIX OPTIONS and opens NO PR (ai-video-reel #26 AT Run 300; Enrollment #24 retire-stale-main, Run 303/294). ***POSTURE SHIFT CONFIRMED (Run 301) AND SUSTAINED (Run 304-305): fully (i)-QUALIFIED, self-contained, PROVABLE, base=running-default, MERGEABLE/CLEAN fixes are sitting HELD as non-draft PRs — Enrollment #25/#26, jobscout #25, and now agent_I_content #2 (caption-limit, tsc+7/7 green, +122/-9). Run 300 flagged #25 as a possible (i)->(ii) DOWNGRADE; Run 301 RESOLVED it (#25 stayed held across a FULL codex run) and Runs 304-305 RE-CONFIRMED the posture (jobscout #25 + agent_I_content #2 both shipped-and-held). So the (i)/(ii) BOUNDARY tracks codex's MERGE-AUTHORITY POSTURE, now DURABLY defer-to-owner. Read: a clean provable fix now surfaces as a HELD PR, NOT an autonomous merge; 'held' no longer implies 'blocked on a live fact / owner decision' — it may just mean 'codex chose not to self-merge'. The founder held-PR queue keeps GROWING even on trivially-mergeable fixes until Stef reviews+merges them.*** ***ISSUE-ONLY->HELD-PR PROMOTION (Run 309/299, ai-video-reel #26->PR#27): a (iii) issue-only hold is NOT permanent — when codex can PROVE the fix DIRECTION with its own evidence (ENUMERATE ALL 26 routes to show the 2 fail-closed guards are the sole outliers), the 'owner must pick the direction' uncertainty DISSOLVES and codex ships a PR — which then HOLDS as (ii) for the residual owner sign-off.*** ***HELD->MERGE PROMOTION — TWO PATHS (a held (ii) PR is NOT permanently stuck):*** PATH (a) MOOT-THE-DEPENDENCY via a BY-CONSTRUCTION safety/correctness argument (Community #13, Run 304/294: 'schema-safe by construction' => the owner schema-check was MOOTED => merged under repo autonomy). PATH (b) SPLIT-ARTIFACT-FROM-ACTION (forming-paws #68, Run 305/295: the migration SQL + trigger is a mergeable CODE ARTIFACT with CI+Vercel green, while RUNNING it against the live DB is a SEPARATE owner operational step => codex merged the code + posted a NOT-YET-LIVE apply-me caveat). Path (a) drains the held PR with NO residual; path (b) drains it but leaves a NEW post-merge 'apply-me' operational item. ***PATH-(b) PRE-STEP — PROVE-THE-SPLIT (Run 308, psychic #1): when a held PR BUNDLES the titled artifact with an unrelated subsystem, codex can first PROVE the bundle is cleanly separable (file-manifest + 0 import edges) and hand the owner exact git commands to cut a titled-only branch — WITHOUT merging, because a SECOND live dependency (Formspree env) still gates merge.*** So the three-way gradient is DYNAMIC: (iii)->(ii) via direction-proving evidence; (ii)->(i) via mooting-proof or artifact/action-split; AND (i)->(ii) via a merge-authority-posture shift — the last now CONFIRMED durable. KEY read: a clean (i)-MERGE or a held->merge promotion drains an audit finding WITHOUT the founder MERGING; an (iii)->(ii) promotion OR an (i)->(ii) posture-downgrade does NOT drain it but puts it in the 'owner reviews+merges' queue. The founder held-PR queue GREW 1->2 at Run 299, 2->3 at Run 300, 3->4 at Run 301, 4->5 at Run 304, and 5->6 at Run 305 — now psychic #1 + ai-video-reel #27 + Enrollment #25 + Enrollment #26 + jobscout #25 + agent_I_content #2.
- ENROLLMENT (RESOLVED Run 282; RE-OPENED-AND-RESOLVED Run 301/292 via #22/#23; RECONCILE FILED Run 303/294 as #24; SIBLING-DRIFT FIX SHIPPED Run 310/300 as #25, HELD; THIRD PARSE-DRIFT FIX SHIPPED Run 311/301 as #26, HELD): both original bugs squash-merged 474c539 to DEFAULT (claude/keen-noether-VED1j). Run 301/292: metric bug (issue #22 = GA4 row count as 'sessions', 106x undercount — METRIC-MISLABEL CLASS), PR #23 MERGED (0d113c932f @2026-09-11T15:43:00Z, closingRefs=[22] => #22 auto-CLOSED). Run 303/294: default already SUPERSEDES main (main is stale/inferior), filed issue #24 (OPEN): retire stale main, do NOT merge main->default (would regress .lt to .lte). Run 310/300: SIBLING-DRIFT bug — PR#23's comma-tolerant parse added to reporter.ts but NOT to claude-client.ts topUtmSources() (Number('1,024')->NaN); shipped PR#25 (fix/utm-sessions-comma-parse, base=default, MERGEABLE/CLEAN, closingRefs=[]) unifying BOTH on shared parseSessionCount() + repo's FIRST test (10/10). NOT self-merged. Run 311/301: THIRD parse-drift — agent.ts CSV parse missing trim:true vs platform csv-normalizer parseCSV => hand-touched export with delimiter-spaces yields key ' Sessions', reporter.ts r['Sessions'] misses it, hasSessionColumn=false, report collapses to row-count fallback (proven 0 vs 458); shipped PR#26 (fix/enrollment-csv-trim-parity, base=claude/keen-noether-VED1j=default, MERGEABLE/CLEAN, 3 files +121/-2, reviewDecision EMPTY, closingRefs=[] — no backing issue) extracting shared trim-enabled helper src/lib/enrollment-csv.ts + test/enrollment-csv.test.ts (tsx 6/6) + tsc clean + sample run unchanged 532. NOT self-merged; ZERO file overlap with #25 => both merge cleanly. See DUPLICATED-LOGIC-DRIFT CLASS (now fired 3x in this repo) + [[agent-repos-duplicated-logic-drifts]]. RESIDUAL for Stef: (1) REVIEW + MERGE BOTH #25 AND #26 (self-contained, base=default, non-overlapping); (2) issue #24 topology reconcile (retire stale main — NON-blocking HYGIENE). Repo now 2 open PRs (#25 + #26) / 1 open issue (#24).
- AGENT_I_CONTENT / CONTENT PIPELINE (Run 305 — PR#2 shipped-and-held): default=claude/eloquent-edison-aF7yG (a claude/* agent default). codex Run 315 shipped PR#2 (fix/enforce-caption-char-limits, base=claude/eloquent-edison-aF7yG==default GOOD-CITIZEN, MERGEABLE/CLEAN, +122/-9 across 4 files [agents/content-pipeline/generator.ts, types.ts, caption-limit.test.ts, package.json], reviewDecision EMPTY, closingRefs=[] — no backing issue, createdAt 2026-09-14T05:02:51Z, NOT self-merged) fixing PROMPT-DECLARED-LIMITS-UNENFORCED: CAPTION_LIMITS were declared to the LLM but only hashtags were code-clamped; caption overshoot flowed into Supabase text_outputs + Remotion video_outputs => broken layout / publish rejection. Added enforceCaptionLimit() (word-boundary truncate + ellipsis, guaranteed <=limit) applied per-post w/ warn log; tsc strict exit 0 + 7/7 tests green. This is the [[agent-repos-prompt-declared-limits-unenforced]] memory firing LIVE and a FLAG-THEN-SHIP instance (memory predicted 'Content Pipeline captions vs hashtags'). FOUNDER: review + merge PR#2 (self-contained, base=default=running branch — reaches deployed code with zero strand risk). This is the 6th held PR. NOTE: agent_I_content is the REPO housing the content-pipeline loop agent's codebase (agents/content-pipeline/**) — distinct from the content-pipeline BUS AGENT that generates the daily LinkedIn posts (#320 as of Run 305). WATCH: does codex mine agent_I_content for further prompt-vs-code constraint gaps (grep every declared limit for a code-side guard)?
- AI-VIDEO-REEL-GENERATOR (#25 RESOLVED Run 284 — MERGED e0d9ac1 @2026-09-10T04:59:05Z base=main; RE-OPENED via issue #26 Run 300/291; SHIPPED-AS-PR#27 Run 309/299): the 13-day FALSIFICATION CASE (#25) is CLOSED. Run 300/291: codex deep-reviewed the publish flow and found a GENUINE regression (issue #26): POST /api/upload-video + DELETE /api/persona use a fail-closed `!secret ||` ADMIN_SECRET guard but are browser-only callers => 401 in every config => Schedule-for-Publishing is DEAD + Remove-persona a silent no-op (see FAIL-CLOSED-GUARD CLASS). Run 309/299: codex SHIPPED PR#27 (fix/remove unsatisfiable guard; base=main, LIVE, MERGEABLE/CLEAN, 3 files +15/-11, reviewDecision EMPTY, closingRefs=[26] POPULATED => #26 auto-closes on merge; enumerated all 26 routes to prove the 2 outliers, kept the path-injection guard, fixed SETUP.md, tsc+build green) and HELD for owner because it reverses hardening PRs #20/#14. RE-CONFIRMED Run 301-305: PR#27 still OPEN/non-draft, issue #26 still OPEN. FOUNDER: REVIEW + MERGE PR#27 (the sole remaining decision is owner sign-off on reversing the hardening). RESIDUAL (Stef): ai-video-reel Supabase issue #5 OPEN, DOUBLE-blocks the content loop AND verifying the #26/#27 fix end-to-end. default IS main (stated by codex + closingRefs corroboration) — no strand/reconcile risk.
- FORMING-PAWS (Track-2 Puppy Power, live prototype; default IS `main`): #64 MERGED into main 2026-09-04T06:19:32Z (06fa9dd); #65 AUTO-CLOSED 06:19:35Z (was base=feat/sage-full-body) — its homepage copy/floor/scroll-cue work ORPHANED off main (FOUNDER: reopen #65 + retarget to main, or cherry-pick head=feat/homepage-copy-and-scroll-cue onto main). Run 298/289: codex MERGED PR #66 (ac10047, base=main) — TIMEZONE FUTURE-GUARD fix, 198/198 + CI + Vercel. Run 299/290: codex found a GENUINE STALE-MIRROR bug (issue #67) + opened PR #68 (fix/sync-owners-email-on-auth-change, base=main, closes #67, migration) and HELD MERGE (Run 290-294). RESOLVED Run 305/295: codex MERGED #68 (0e9a1ceb @2026-09-12T08:15:05Z, base=main=default, reviewDecision EMPTY, closingRefs=[67] => #67 auto-CLOSED 08:15:06Z) — promoted held->merge via PATH (b) artifact/action-split: merged the trigger+migration CODE (CI+Vercel green) and deferred the live-apply to the owner. codex renumbered the migration 0028 -> 0032 (main already shipped 0028_admin_dog_management.sql — DUP PREFIX) and posted a NOT-YET-LIVE caveat: owner must apply 0032 BY HAND via execute_sql + run the assertion suite (this repo applies migrations manually, ledger is NOT source of truth). Repo now 0 open PRs / 1 open ISSUE = #8 (certified Run 299: 'Legal docs ready (Plan Day 9): IL articles of incorporation + bylaws drafted' — a LEGAL/BUSINESS milestone, Track-2 Forming Paws execution, NOT a code fix; codex Run 309 mislabeled it 'owner-blocked' alongside code holds — do NOT carry it in the code queue). RESIDUALS for the founder: (1) APPLY 0032 (execute_sql + assertions — the post-merge apply-me item); (2) the #65 orphan; (3) Track-2 business milestone #8. See [[agent-repos-migrations-applied-by-hand]].
- MERGE STALL BROKEN + CADENCE (through Run 315/305): SEVENTEEN fix-queue merges in the Runs 259-305 window across EIGHT repos (forming-paws #64 + #66 + #68; psychic #20/#22/#19; jobscout #21/#20/#22 + #24; Community #11 + #13; Enrollment #20/#14/#21 + #23; ai-video-reel #25) — NO NEW MERGE at Runs 296-305 (codex Run 307 PUSHED psychic #1 fixes; Run 308 posted psychic #1's scope-split manifest; Run 309 SHIPPED ai-video-reel PR#27 but held it; Run 310 SHIPPED Enrollment PR#25 but held it; Run 311 SHIPPED Enrollment PR#26 but held it; Run 314 SHIPPED jobscout PR#25 but held it; Run 315 SHIPPED agent_I_content PR#2 but held it; none is a merge). OPERATIONAL ARC: merge authority was the bottleneck (moved 17x), then effort shifted MERGING -> UNBLOCKING -> AUDIT/DISCOVERY -> SHIP-FIX-BUT-HOLD -> AUDIT-THEN-MERGE -> AUDIT-THEN-HOLD-PR-AGAIN -> AUDIT-THEN-FILE-ISSUE-ONLY -> AUDIT-THEN-MERGE-AGAIN -> ... -> AUDIT-A-THIRD-SAME-PIPELINE-PARSE-DRIFT-THEN-SHIP-BUT-HOLD (Enrollment #26, Run 311) -> AUDIT-A-SALARY-SCORER-BUG-THEN-SHIP-BUT-HOLD (jobscout #25, Run 314) -> AUDIT-A-PROMPT-VS-CODE-CONSTRAINT-GAP-THEN-SHIP-BUT-HOLD (agent_I_content #2, Run 315). Working theory = the PROVABLE-vs-UNVERIFIABLE axis, a DYNAMIC THREE-WAY HOLD GRADIENT (merge / held-PR / issue-only) where items flow (iii)->(ii) via direction-proving evidence, (ii)->(i) via mooting-proof or artifact/action-split, AND (i)->(ii) via a merge-authority-posture shift — the posture shift now CONFIRMED DURABLE across Runs 300-305 (five clean provable base=default fixes shipped-and-held). The founder's blocked queue is now 6 held PRs (psychic #1 + ai-video-reel #27 + Enrollment #25 + Enrollment #26 + jobscout #25 + agent_I_content #2) + 1 post-merge apply-me (forming-paws 0032) + 1 infra (#5, double-blocker); the DECIDE-THEN-FIX(no-PR) bucket is EMPTY. WATCH: does codex keep finding AUDIT->FIX bugs on already-reviewed AND new surfaces (Enrollment yielded 3 parse-drifts; jobscout RE-OPENED after 'done'; agent_I_content is a fresh repo surface), does the defer-to-owner posture hold (CONFIRMED through Run 305) or does codex resume self-merging, and does the founder queue finally drain when Stef provisions the Formspree env + reviews the SIX held PRs?
- FLAG-THEN-MERGE / FLAG-THEN-CLEAR / FLAG-THEN-SHIP SIGNATURE: a flagged item keeps getting merged, consolidated-away, UNBLOCKED, FIXED-VIA-NEW-PR, or PROMOTED-FROM-ISSUE-TO-PR shortly after the flagging run. Run 287 flagged Community #12 -> Run 304/294 MERGED #13 (closed #12). Run 290 flagged forming-paws #67/#68 held -> Run 305/295 MERGED #68 (closed #67). Run 291 flagged ai-video-reel #26 issue-only -> Run 309/299 SHIPPED PR#27 (issue-only->held-PR). The [[agent-repos-duplicated-logic-drifts]] memory (written after PR#23) PREDICTED the claude-client.ts sibling gap -> Run 310/300 codex SHIPPED PR#25; and the SAME memory-flagged 'audit EVERY sibling' lesson pre-figured the THIRD drift -> Run 311/301 codex SHIPPED PR#26 (CSV-trim, a DIFFERENT parse stage). The [[agent-repos-prompt-declared-limits-unenforced]] memory (Content Pipeline captions vs hashtags) PREDICTED the caption gap -> Run 315/305 codex SHIPPED agent_I_content PR#2 (enforce CAPTION_LIMITS). psychic #1: Run 284 flagged rebase -> Run 295 rebase cleared -> Run 307 fixes pushed in-branch -> Run 308 scope-split proven (still held on Formspree env). Prior: Run 278 jobscout #21; Run 279 Community #11 + Enrollment #20; Run 280->281 Enrollment #14; Run 281->282 Enrollment #16/#18; Run 282 jobscout #20; Run 283->284 jobscout scorer stack via #22 + ai-video-reel #25. LESSON: flag-then-clear is a tendency in AGENT-ACTIVE repos; an agent-less flagged item waits until an agent shows up. Re-verify vanished/flipped PRs by name (mind the leading-dash repo) + read the feed tail after posting.
- SEQUENTIAL-CONFLICT — LIVE INSTANCES (carried, re-check each loop): (1) psychic #1 — RESOLVED Run 295 as a rebase item (re-certified HELD MERGEABLE/CLEAN base=main, 34 files as of Run 307-308, reviewDecision EMPTY, ~92 days old): no longer a rebase item — it is MERGE-PENDING (Formspree env + run codex's split-commands + merge; Stef; Track-1 public content PR). (2) jobscout #19 — RETIRED Run 284: codex discarded it and built clean #22.
- JOBSCOUT (Run 284 open-PR queue exhausted; Run 302/293 STRANDED-FIX RESOLVED via #24; RE-OPENED Run 304 via #25, HELD): default=claude/clever-cannon-IDh3G (NOT main). Two consolidators merged to main earlier (#20 reporter.py; #22 scorer.py f30637f), BUT they were STRANDED on main — the running default never got them. codex Run 302 caught the live /scan running the OLD buggy scorer (5 bugs, worst: 'il'-substring location false-positives), ported main's fixed scorer/config/reporter + 10-test suite onto default via PR #24 (closes #23, MERGED 53474352cf @2026-09-11T19:51:45Z, ~21s, zero-loss). main<->default STILL diverged (ahead 8/behind 6) — default carries unique remote-control + /dd skills main lacks => a topology-reconciliation item for Stef (NON-blocking). RE-OPENED Run 304: codex Run 314 shipped PR #25 (fix/salary-range-placeholder-and-shared-k, base=default=claude/clever-cannon-IDh3G GOOD-CITIZEN, MERGEABLE/CLEAN, +111/-8 across 2 files, closingRefs=[], NOT self-merged) fixing _salary_score(): it read only the FIRST number, so '$0 - $200k DOE'->0.000 (placeholder-0 low) and '$85 - $110k'->0.002 (shared-k upper bound ignored) dragged fit scores down / risked filtering good roles out; now parses all figures w/ optional k, shares a range-k to bare sub-1000 bounds, conservative low w/ placeholder-0 fallback that 401k/PTO/stray-year cannot hijack; +tests/test_salary_range_parsing.py, full suite 11/11 green. SCORER-CORRECTNESS sibling of the 'il'-substring location bug => jobscout fix queue NOT exhausted; jobscout #25 is a held PR (5th at Run 304). See STRANDED-FIX-ON-NON-DEFAULT-BRANCH CLASS + PROVABLE-vs-UNVERIFIABLE (i)->(ii) posture.
- AUTO-CLOSE THEORY (Run 279, CORRECTED Run 282, HELD 283-305): closingIssuesReferences-populated auto-close IS reliable. body-'Closes #N' DOES auto-link and auto-close ON MERGE-TO-DEFAULT (proven Enrollment #21 -> #15/#17; Enrollment #23 -> #22 @Run 292; JobScout #24 -> #23 @Run 293; Community #13 -> #12 @Run 304/294; forming-paws #68 -> #67 @Run 305/295, closingRefs=[67] POPULATED + base=main=default + #67 CLOSED 1s after merge => auto-closed). TITLE-'Closes #N' NEVER auto-linked (Enrollment #14). closingRefs-EMPTY merges do NOT auto-close (forming-paws #66 closingRefs=[]; Enrollment #25 + #26 closingRefs=[]; jobscout #25 closingRefs=[]; agent_I_content #2 closingRefs=[] — no backing issue, so nothing to close). RULE: to guarantee auto-close, put 'Closes #N' in the PR BODY (or title-with-closingRefs-populated) and merge to the DEFAULT branch. Certify closingIssuesReferences per-PR EVERY loop. ALSO: closingRefs POPULATED on a PR is itself proof base==default (GitHub only auto-links closing refs for default-targeted PRs) — corroborated AGAIN by #68 (closingRefs=[67] + base=main) AND by ai-video-reel PR#27 (closingRefs=[26] + base=main); the converse is NOT true — closingRefs-EMPTY does NOT imply non-default (Enrollment #25/#26, jobscout #25, AND agent_I_content #2 all have closingRefs=[] yet base==default, confirmed DIRECTLY via defaultBranchRef); confirm base vs defaultBranchRef directly.
- REVIEW-NOT-REGISTERING (holds Run 305): codex bus claims of 'posted approving review' do NOT register on GitHub — psychic #1 reviewDecision RE-CERTIFIED EMPTY + reviews=[] EMPTY Run 286-299; Enrollment #23 reviewDecision=EMPTY at merge (Run 292); JobScout #24 reviewDecision=EMPTY at merge (Run 293); Community #13 reviewDecision=EMPTY at merge (Run 304/294); forming-paws #68 reviewDecision=EMPTY at merge (Run 305/295); ai-video-reel PR#27 reviewDecision=EMPTY as shipped (Run 309/299); Enrollment #25 reviewDecision=EMPTY as shipped (Run 310/300); Enrollment #26 reviewDecision=EMPTY as shipped (Run 311/301); jobscout #25 reviewDecision=EMPTY as shipped (Run 314/304); AND agent_I_content #2 reviewDecision=EMPTY as shipped (Run 315/305). On a MERGE codex bypasses the review entirely by direct founder-token merge; on a HELD PR the founder gets NO green signal from a codex review/comment — the PR needs a REAL approving review or a direct founder merge. Certify reviewDecision on GitHub, never trust the bus claim.
- PSYCHIC (Run 301): MERGEABLE fix queue EXHAUSTED and repo AUDIT-CLEAN (codex Run 308 deep-reviewed forms/routes/mailer, NO new bug). open surface 2 PRs / 1 non-draft [#1 live MERGE-PENDING; #11 grant-tracker draft — stays draft]. #20/#22/#19 all MERGED. #1: base=main, non-draft, MERGEABLE/CLEAN, 34 files (+7804/-52), reviewDecision EMPTY, ~92 days old, Track-1 Plug AI public content. CODE-COMPLETE (Run 307: 3 a11y/SEO fixes + fail-visible lead-loss guard, build-verified) + SPLIT-PROVEN + TRIPLE-CORROBORATED (Run 308 codex manifest, Run 298 antigravity, Run 321 clawd shallow-clone+grep: 34 = 15 site + 16 content-engine/** + 3 docs, 0 import edges, root tsconfig excludes content-engine). NOTE (Run 305 sweep): psychic #11 grant-tracker MERGED earlier (off queue, non-fix-queue) and a NEW psychic #25 /websites-services DRAFT is present (stays draft — non-fix-queue). FOUNDER: (1) provision Formspree env (HARD functional prereq — the fail-visible guard means no env = broken unlock), (2) run codex's split-commands to cut a site-only launch branch, (3) merge — a held PR in the founder-blocked queue.
- COMMUNITY_INTAKE (PR #11 MERGED Run 279; ISSUE #12 filed Run 287; FIX PR #13 shipped Run 288, held Run 288-293, MERGED Run 304/294 => fix queue EXHAUSTED): PR #13 (fix/web-intake-reasoning-parity, base=default claude/quirky-galileo-UGnfz) MERGED f180b409 @2026-09-12T04:05:45Z, closingRefs=[12] => #12 auto-CLOSED @04:05:46Z, reviewDecision EMPTY (direct founder-token merge). The prior OWNER-BLOCK was MOOTED: 'schema-safe by construction'. codex Run 310/311 RE-CONFIRMED -Community_intake_Routing clean/green (42 tests) as a side-check. Repo now 0 open PRs / 0 open issues. See HELD->MERGE PROMOTION path (a) + CROSS-IMPL DATA-LOSS CLASS.
- OTHER OPEN SURFACE (Run 305 sweep, all DRAFT / non-fix-queue — not founder-actionable as merges): content-machine 4 drafts (#3/#4/#5/#10); Link-inbio #5/#6 draft + #15 LIVE (Command Center scroll dashboard rebuild — a build PR, not the small-diff fix queue); kai-obsidian-vault #2/#3 draft; psychic-bassoon #25 draft (/websites services page); ----Workspace-notes #1 draft (Master Vault). These are build/vault PRs, not the small-diff fix queue; leave to their owning agents/founder. (obsidian-kai #2 MERGED @2026-09-13T19:28Z — non-fix-queue vault, off the open surface.)
- ACTIVE-PRIORITIES DIGEST (Run 305, from context.md mtime 2026-07-29 + world.md mtime 2026-07-23 — BOTH statically UNCHANGED for weeks): Track-1 = The Plug AI (AI literacy, south-suburban Cook County). SEQUENCING: Plug AI execution DEFERRED until AFTER OpenClaw Phase 1-4 completes (target 2026-10-12); Plug AI build starts 2026-10-13, fiscal-sponsor target 2026-11-30. IL Digital Equity $75K grant LAPSED BY CHOICE (do NOT re-flag). LIVE operational surface right now = cross-repo fix queue + vault upkeep, NOT new Track-1 feature build. Track-2 active: BigHeart content automation + title/comp ask; Puppy Power / Forming Paws execution. FOUNDER QUEUE NOW: free-land PR bucket EMPTY; rebase-then-merge bucket EMPTY; DECIDE-THEN-FIX(no-PR) bucket EMPTY. MERGE-PENDING = SIX non-draft held PRs (as of Run 305): (1) psychic #1, CODE-COMPLETE + SPLIT-PROVEN (Formspree env + run split-commands + merge, Stef, Track-1 public content); (2) ai-video-reel #27 (review + merge; reverses hardening #20/#14 so held for owner sign-off; base=main; closingRefs=[26] => auto-closes #26); (3) Enrollment #25 (review + merge; comma-thousands sibling fix, base=default=running branch, closingRefs=[]; NOT self-merged); (4) Enrollment #26 (review + merge; CSV-trim parity fix, base=default, closingRefs=[], ZERO overlap with #25 => merge BOTH; NOT self-merged); (5) job_opportunity_scanner #25 (review + merge; salary-range scorer fix, base=default=running branch, +111/-8, closingRefs=[]; NOT self-merged); (6) agent_I_content #2 (review + merge; CAPTION_LIMITS enforcement, base=default=running branch, +122/-9, closingRefs=[]; NEW Run 305, NOT self-merged; [[agent-repos-prompt-declared-limits-unenforced]] class). POST-MERGE APPLY-ME (bucket) = forming-paws 0032 migration (owner runs execute_sql + assertion suite; MERGING != APPLYING — [[agent-repos-migrations-applied-by-hand]]). INFRA (Stef) = ai-video-reel Supabase provision (issue #5; DOUBLE-blocks the content loop AND end-to-end #26/#27 verification). Stef residual (NON-blocking hygiene) = TWO main<->default reconciliations (Enrollment: issue #24 — retire stale main, default supersedes it; JobScout: main<->default diverged ahead 8/behind 6, default carries unique remote-control + /dd skills) + forming-paws #65 orphan (reopen+retarget to main, or cherry-pick head=feat/homepage-copy-and-scroll-cue). Track-2 business milestone (not code) = forming-paws #8 (IL articles of incorporation + bylaws drafted). Community fix queue EXHAUSTED (post-#13); jobscout fix = PR#25 held; Enrollment code fix = PR#25 + PR#26 held (both merge-pending, non-overlapping); forming-paws EXHAUSTED (post-#68; only the 0032 apply-me + #65 orphan + #8 business-milestone residuals remain); psychic EXHAUSTED + AUDIT-CLEAN (post-Run-308); ai-video-reel fix queue = PR#27 held; agent_I_content fix = PR#2 held. ALSO (bus): content-pipeline Post #320 done (water-bill affordability + LEAD SERVICE LINE replacement lane — LIHWAP/CEDA, provider payment plan, ICC/CUB, IL free lead-line-replacement law, free tap test + kids' blood-lead check; counter 319->320; grepped ALL output/, zero prior water/lead posts; self-owned; [[content-pipeline-domain-novelty-dedup]]; re-verify only if drift flagged).
- GOVERNANCE GATE (Run 275, holds 305): STEF-WEEK-1-APPROVAL.md is NOT present anywhere under ~/Desktop/kai (verified via find). Stop carrying it as an unverified 'coldness' gate; treat as archived/relocated unless it resurfaces (check ~/clawd/AI-Organization if re-raised).
</content>
</invoke>
