# Codex Merge Runbook — 2026-09-15 (Run 322)

**Bottleneck:** all 9 open PRs are CLEAN and stalled on **owner merge** — nothing has
merged since run 320. This run did NOT pile a 10th PR. Instead it *test-merged every
multi-PR set locally* (both orders) so the owner has a verified, copy-paste merge sequence,
and surfaced one uncovered post-merge gap.

## Verified merge order (do these in order; each set test-merged clean locally today)

### 1. agent_I_content — a STACK, order matters
`#4` is based on `#2`'s head (not the default). Merge:
```
gh pr merge 2 --repo morrisstephon51/agent_I_content --squash   # caption limits (creates `test` script)
gh pr merge 4 --repo morrisstephon51/agent_I_content --squash   # topic-derived hashtags (extends `test`); auto-retargets to default after #2
gh pr merge 6 --repo morrisstephon51/agent_I_content --squash   # platform validation guard
```
Verified: #2 → #4 → #6 merges CLEAN into `claude/eloquent-edison-aF7yG`; all three fixes + test files land.

**⚠ Post-merge one-liner (do NOT skip):** PR #6 adds `platform-validation.test.ts` but does
**not** wire it into `package.json`. After the merges, `npm test` runs caption-limit +
hashtag-filter only — #6's guard test never executes. Append the third term:
```json
"test": "ts-node agents/content-pipeline/caption-limit.test.ts && ts-node agents/content-pipeline/hashtag-filter.test.ts && ts-node agents/content-pipeline/platform-validation.test.ts"
```
(Detail + rationale posted on PR #6: issuecomment-5678599350.)

### 2. job_opportunity_scanner — #25 ⊥ #27, either order
Both touch `scorer.py` but disjoint hunks; test-merged CLEAN both orders.
```
gh pr merge 25 --repo morrisstephon51/job_opportunity_scanner --squash   # salary placeholder-0 / shared-K
gh pr merge 27 --repo morrisstephon51/job_opportunity_scanner --squash   # "X years ago" recency filter
```
Note: run-321 review comment on #27 (issuecomment-5675620363) flags that article forms
("a year/month ago") still slip past MAX_DAYS_OLD=7. #27's tests deliberately assert the
year case as include — an owner semantic call, deferred. Not a merge blocker.

### 3. Enrollment_Funnel_Agent — #25 ⊥ #26, either order
Fully disjoint files; test-merged CLEAN both orders.
```
gh pr merge 25 --repo morrisstephon51/Enrollment_Funnel_Agent --squash   # GA4 Sessions thousand-separators
gh pr merge 26 --repo morrisstephon51/Enrollment_Funnel_Agent --squash   # CSV trim parity
```

### 4. -Community_intake_Routing — single PR
```
gh pr merge 15 --repo morrisstephon51/-Community_intake_Routing --squash  # drop brand/corporate affiliation nouns
```

### 5. ai-video-reel-generator — single PR
```
gh pr merge 27 --repo morrisstephon51/ai-video-reel-generator --squash    # remove unsatisfiable ADMIN_SECRET guard
```

## Owner-only (no code possible by Codex)
- **Enrollment_Funnel_Agent #24** — retire stale `main` (live default already supersedes it). Git/admin action.
- **ai-video-reel-generator #5** — provision a Supabase project to unblock end-to-end runtime test.

## Facts verified live today (gh + local clones under /tmp, since removed)
- agent_I_content: `#2 ancestor-of #4` = YES (true stack); #6 test orphaned from `test` script = confirmed.
- job_opportunity_scanner & Enrollment: set test-merge CLEAN in both orders.

*Generated by Codex run 322. Supersedes MERGE-TRIAGE-2026-09-13.md.*
