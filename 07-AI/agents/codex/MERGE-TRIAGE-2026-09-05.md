# Codex Merge Triage — 2026-09-05 (Run 280)

**Finding:** Every open GitHub issue across the 4 active code repos already has a ready fix PR.
No un-fixed bug remains in the hot paths — the blocker is **merge, not more code.**

**New risk identified this run:** GitHub reports all PRs `MERGEABLE`, but that only means
"no conflict against the base *as it stands right now*." Several repos stack multiple
independent PRs on the **same base** editing the **same function/file**. The moment the
first one merges, the rest will conflict and need manual rebasing. Safe merge order matters.

---

## 1. Community_intake_Routing — ⚠️ SEQUENTIAL-CONFLICT STACK
All four PRs share base `claude/quirky-galileo-UGnfz` and all rewrite `classify()` in
**both** `intake.js` and `api/intake.js`:

| PR | Fix | Issue |
|----|-----|-------|
| #2  | word-boundary keyword match (no substring misroute) | #3 |
| #6  | neutral confidence for no-signal learner default | #5 |
| #8  | exclude `how_heard` from intent scoring | #7 |
| #10 | score winner vs evidence denominator (single-keyword partner/volunteer) | #9 |

**These 4 will collide after the first merge.** They are logically orthogonal (matching,
confidence, input text, denominator) and combine cleanly. **Recommendation:** either
(a) merge in order #2 → #6 → #8 → #10, rebasing each after the prior, or
(b) codex opens ONE consolidated PR folding all four into a single `classify()` — mirror
what was already done for job_scanner (PR#19). Option (b) is the lower-effort path for the founder.

## 2. job_opportunity_scanner — ✅ FULLY CONSOLIDATED (update: run 283, 2026-09-06)
- **scorer.py / config.py stack:** PR#7–#13 consolidated into **PR#19** (`fix/scorer-consolidated`,
  base `main`; scorer.py+config.py+`tests/test_scorer_consolidated.py`).
- **reporter.py stack:** NOW consolidated into **PR#20** (`fix/reporter-consolidated`, base `main`;
  reporter.py + the 3 reporter tests). The triage's original "STILL NOT consolidated" note is stale.
- **PR#18 (`fix/salary-score-monthly-weekly-periods`, base `main`):** superseded by #19.
  Verified run 283 by diffing `_salary_score`: #19 is a **strict superset** of #18 — same pay-period
  normalization (#17) **plus** the `$85k` k-notation fix (#9) that #18 lacks (#18 still uses bare
  `re.findall(r"\d+")`). Merging #18 over #19 would *regress* #9. Direction: merge #19, close #18 —
  never the reverse (directional warning comment posted on #18).
- **Recommendation:** merge **#19 then #20 to main** (disjoint files, either order), then CLOSE
  #7,#8,#9,#10,#11,#12,#13,#18 (scorer → #19) and #14,#15,#16 (reporter → #20) as superseded.
  Open **issue #17** is fixed by #19 and closes on that merge.

## 3. Enrollment_Funnel_Agent — ✅ NO CONFLICT (disjoint files)
PR#20 `supabase.ts`, #14 `csv-normalizer.ts`, #18 `reporter.ts`, #16 `agent.ts` — four
**different files**, no overlap. Safe to merge in any order. Only caveat: #20 and #14 are
off base `claude/keen-noether-VED1j`; #18 and #16 are off `main`. Merge the base branch first,
then the two main-based PRs.

## 4. psychic-bassoon — ⚠️ lib/utils.ts overlap
PR#22 (utils date fix), PR#1 (content), and PR#11 (grant tracker) all touch `lib/utils.ts`
(+ `.env.example` for #1/#11). #20 (Next bump) and #19 (security headers) are independent/safe.
**Recommendation:** land the small independent ones first (#19, #20, #22), then reconcile the
two large feature branches (#1, #11) against the updated `lib/utils.ts`.

---

## Bottom line for the founder
1. Nothing new to *code* on the known issues — they're all fixed and waiting.
2. Merge the **consolidated** PRs where they exist (job_scanner #19), close the superseded ones.
3. For the remaining stacks (Community_intake ×4, job_scanner reporter ×3), pick: sequential
   rebase-merge, or ask codex to consolidate (recommended — one review instead of four).
4. Enrollment is clean; merge its base branch, then the main-based PRs.

*Generated by Codex run 280. Verified via `gh pr list --json mergeable,baseRefName,files`.*
