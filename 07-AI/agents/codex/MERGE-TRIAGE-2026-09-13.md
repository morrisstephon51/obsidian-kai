# Codex Merge Triage — 2026-09-13 (Run 312)

**Method:** cloned + read all 6 active repos; verified every open PR's files, `mergeable`,
and base freshness with `gh`; confirmed base-branch code state by cloning the *live default*
branch (not `main`) per the known "fixes stranded on main" risk.

## Headline
No un-fixed, provable bug remains in any hot path — the codebases are genuinely hardened.
Every known issue is either merged or has a CLEAN pending PR. The blocker is **merge**, and
the one live *merge-safety landmine* is in **psychic-bassoon** (below).

## psychic-bassoon — ⚠️ add/add conflict on `.env.example` (NEW; corrects run 280)
- PR #1 (content/forms) and PR #11 (grant tracker) **both create `.env.example` as a new file**
  (`main` has none — `GET contents/.env.example` → 404). Both show `MERGEABLE` only because
  GitHub compares each vs current `main`.
- **Whichever merges second hits an add/add conflict.** Keys are **disjoint** (Formspree vs
  Supabase/Groq/cron/SMTP), so a "pick one side" resolution silently drops the other PR's env
  vars → Formspree forms die, or grant endpoints 503 on missing `MUTATION_SECRET`.
- **Action taken:** posted a merge-safety comment with the ready-to-paste **unioned**
  `.env.example` on both PRs (#1 comment 5654539211, #11 comment 5654538945).
- **Correction:** the 2026-09-05 triage listed `lib/utils.ts` as the #1/#11 collision — that
  file is in **neither** PR now (both were updated since). `.env.example` is the only overlap.

## Enrollment_Funnel_Agent — ✅ #25 ⊥ #26 verified disjoint
- #25 files: `package.json`, `src/lib/claude-client.ts`, `src/lib/reporter.ts`, `test/sessions.test.ts`
- #26 files: `src/agent.ts`, `src/lib/enrollment-csv.ts`, `test/enrollment-csv.test.ts`
- **Zero overlap** — both CLEAN on base `claude/keen-noether-VED1j`; merge in any order.
- Session-parse family fully covered: #23 (merged: `reporter.ts buildReport`) + #25 (ranking in
  `claude-client.ts topUtmSources`) + #26 (`agent.ts` CSV read trim). No un-covered sibling site
  (checked every `Number()/parseInt/parseFloat` call — all handle comma+trim or are covered).
- Issue #24 (retire stale `main`) = infra decision, no code.

## ai-video-reel-generator — ✅ #27 covers both ADMIN_SECRET outliers
- PR #27 (base `main`, CLEAN) drops the unsatisfiable fail-closed guard from **both**
  `upload-video` POST and `persona` DELETE (the persona DELETE was NOT missed — verified in the
  PR body + diff). `cron/publish` correctly keeps its soft `CRON_SECRET` guard.
- Issue #5 (Supabase project) = owner setup, blocks end-to-end runtime test only.

## job_opportunity_scanner — ✅ live branch synced, no stranded fixes
- Live default `claude/clever-cannon-IDh3G` vs `origin/main`: only `.claude/` skill files + a
  `.gitignore` line differ — commit #24 already ported the consolidated scorer/reporter fixes to
  the live branch. `scorer.py`/`reporter.py`/`config.py` byte-identical across both. No open PRs/issues.
- `_keyword_score` bare-substring is safe: every `SEARCH_KEYWORD` is a distinctive multi-word phrase
  (no short-token false-positive risk like the one fixed in `_title_score`).

## -Community_intake_Routing — ✅ classify() parity holds
- CLI `intake.js` and `api/intake.js` `classify()` are functionally identical (CLI's
  `weight:1.0`×match == API's `+=1`; both end at `learner=0.5` baseline, same evidence-only
  denominator, same confidence/threshold, same reasoning string). No drift. No open PRs/issues.

## forming-paws — issue #8 = legal docs (non-code, owner).

*Generated by Codex run 312. All PR/branch facts verified live via `gh` + clones on 2026-09-13.*
